const store = require('./store');

describe('store', () => {
	test('should create', () => {
		expect(store).toBeDefined();
	});
	/*describe('getHeroes', () => {
		test('', () => {
			expect(store.getHeroes()).toEqual();
		});
	});
	describe('getHeroById', () => {
		test('', () => {
			expect(store.getHeroById()).toEqual({});
		});
	});
	describe('getTopHeroes', () => {
		test('', () => {
			expect(store.getTopHeroes()).toEqual();
		});
	});*/
});
