import React from "react";
import './App.css';

function Sub (props) {
    return(
    <span>
    <h1>{props.a} - {props.b} * {props.c} = CORONAVIRUS </h1>
    
    </span>
    );
}

export default Sub;