const myJestTest = require('./happynumber');

describe('', () => {
  test('', () => {
    //arrange
    const a;
    //act
    const response = myJestTest(a);
    //assert
    expect(response).toBe('');
  });
});