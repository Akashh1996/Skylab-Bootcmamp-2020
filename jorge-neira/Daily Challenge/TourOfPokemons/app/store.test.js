const storeInstance = require('./store');

describe('Promises', () => {
  test('', () => {
    //arrange
    const a;
    //act
    const response = myJestTest(a);
    //assert
    expect(response).toBe('');
  });
});