const dashboard = require('./dashboard');

describe('Dashboard', () => {
	test('create link top heroes', () => {
		const createElem = document.createElement('a');
		//act
		const response = a;
		//assert
		expect(response).toEqual();
	});
});
