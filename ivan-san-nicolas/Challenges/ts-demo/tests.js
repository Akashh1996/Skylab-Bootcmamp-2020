const num = 24;
function numberToString(number) {
  const string = `${number}`;
  return string;
}
console.log(numberToString(num));
const npc = {
  name: '12',
  race: 'Human',
  legs: 'two',
};
console.log(npc.legs);
