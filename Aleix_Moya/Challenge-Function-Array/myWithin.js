const skylabArray = {
    array: {
        1: "a",
        2: "b",
        3: "c",
        4: "d",
        5: "e"
    },
    
    myWithin : function callback(callbackFunction) {
        var index = 0;
        Object.entries(this.array).forEach( (property)=>{
           
        });
        return ;
    }
    
}
Object.prototype.myWithin
function myWithin(target, start = 0, end = this.array.length){

}
skylabArray.myFill.call(skylabArray, myWithin);
