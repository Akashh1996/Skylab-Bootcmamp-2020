/* eslint-disable no-undef */
// eslint-disable-next-line spaced-comment
/*const Wargear = require('./myStore');

let { savedProducts } = Wargear;

describe.only('Store', () => {
  test('Return datasheet', () => {
    Wargear.getDatasheet();
    expect(datasheet).toBeDefined();
  });
  test('Should push weapon to carrito when empty', () => {
    const weapon = { weapon: 'brrr' };

    savedProducts = Wargear.addToCarrito(weapon);

    expect([{ weapon: 'brrr' }]).toEqual(savedProducts);
  });
});*/
