{
	"name": "strict-equals",
	"version": "1.0.0",
	"description": "Function that simulates strict equality without using it.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"jest": "^26.5.3"
	}
}
