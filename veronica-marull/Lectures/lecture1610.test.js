const {customArray, SkylabArray} = required('./lecture1610');

describe('array-methods', () => {
    describe('length', () => {
        test('should have a default length equals 0', () => {
            //arange
            //act
            //assert
            expect(customArray.length).toBe(0);
        });
    })


})