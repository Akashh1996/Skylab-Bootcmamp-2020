const {startGame, nextGen} = require('./gameOfLife');

describe ('Game of life', () => {
    test('should return', () => {
         //arrange
         nextGen()
         
 
         //act
         const response = nextGen();
 
         //assert
         expect(response).toBe([

            
         ]})
   
 
})