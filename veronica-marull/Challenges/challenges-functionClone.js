const myDog = {

    name: "Uma",
    age: 4
    
}

function makeClone(anObject) {

    const obj = anObject;

    let cloneProperties = Object.keys(obj);
    let cloneValues= Object.values(obj);
    const clone = {}

    for (e of cloneProperties) {

        
    }
    for (let indexValue = 0; indexValue < cloneValues.length; indexValue++ ){
            


        clone = {

            e: 0;
        }
        indexValue++;
        
        break;
        
    } 

    return clone;


}

