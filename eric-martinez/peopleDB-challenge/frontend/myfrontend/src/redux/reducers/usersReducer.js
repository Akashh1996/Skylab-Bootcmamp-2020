import actionTypes from '../actions/';

export default function usersReducers(state = {}, action){
    switch (action.type) {
        case actionTypes.L:
            
            break;
    
        default:
            break;
    }
}